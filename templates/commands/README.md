# QA Kit Command Templates

This directory is intentionally reserved for slash command templates.

Command files will be installed into the active AI agent integration directory
by the command registrar.
